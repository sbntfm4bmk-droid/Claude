# Copy to .env and adjust as needed
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production-please"
JWT_EXPIRES_IN="7d"
PORT=4000

# Payments: leave empty to use the simulated PSP (instant success, for dev/demo).
# Set a Stripe secret key to switch to real charges (see src/lib/payments.ts).
STRIPE_SECRET_KEY=""
